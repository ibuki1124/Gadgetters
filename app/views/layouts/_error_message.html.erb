<% if model.errors.any? %>
  <ul class="alert alert-danger">
    <h6 class="alert-heading"><%= model.errors.count %>件のエラーが発生しました。</h6>
    <% model.errors.full_messages.each do |message| %>
      <li class="ml-2"><%= message %></li>
    <% end %>
  </ul>
<% end %>